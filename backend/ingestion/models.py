# Ingestion app has no database models of its own.
# All persisted data lives in emissions.IngestionBatch and emissions.EmissionRecord.
# This file is required for Django to recognise the ingestion app correctly.
